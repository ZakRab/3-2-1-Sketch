import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'three21sketch.onrender.com',
  appName: '\x15Sketch 3, 2, 1,',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
